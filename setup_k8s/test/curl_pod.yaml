apiVersion: v1
kind: Pod
metadata:
  name: curl-test
spec:
  containers:
  - name: curl
    image: curlimages/curl:latest
    command: ["sleep", "infinity"]  # Pod sẽ không thoát tự động và bạn có thể sử dụng nó để thực hiện các lệnh `curl`
