apiVersion: v1
kind: Pod
metadata:
  name: netcat-test
  namespace: default
spec:
  containers:
  - name: netcat
    image: alpine:latest
    command: ["sh", "-c", "while true; do sleep 30; done;"]
    stdin: true
    tty: true
  restartPolicy: Never
